import 'package:flutter/material.dart';

class CursosTab extends StatelessWidget {
  const CursosTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Cursos', style: TextStyle(color: Colors.white, fontSize: 24)),
    );
  }
}
