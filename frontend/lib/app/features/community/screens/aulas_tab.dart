import 'package:flutter/material.dart';

class AulasTab extends StatelessWidget {
  const AulasTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Aulas', style: TextStyle(color: Colors.white, fontSize: 24)),
    );
  }
}
